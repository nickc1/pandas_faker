from setuptools import setup

setup(
	name='pandas_faker',
	author='Nick Cortale',
	version='0.0.1',
	description='pandas_faker is a wrapper around the Faker library that dumps fake data into dataframes.',
	packages=['pandas_faker']

)